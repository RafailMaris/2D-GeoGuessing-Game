	To work with the provided database:
Install SQL tool: DataGrip, DBeaver
Install SQL Software (PostgreSQL)

Download PostgreSQL.

Run the Installer: Launch the installer and follow the instructions. Set a password and username. Note the port number (default is 5432).

Create the needed database:
Use the provided file by copy and paste-ing it into the software. 

Configure project: add into the project, in DBInfo class: password and username

